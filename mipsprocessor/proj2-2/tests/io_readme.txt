The IO device that we created looks at the outputs of the registers after an instruction and counts how many registers store a value. Then in the IOdevice circuit, there are 7-segment displays that light up the corresponding count.

(IOdevice_test.circ and the tick frequency does not matter)



